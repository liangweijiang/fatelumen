"use client";
import { Save } from "lucide-react";

export default function GeneralSettingsPage(){return <section className="max-w-5xl border" style={{borderColor:"var(--line)",background:"var(--bg-card)"}}><header className="border-b px-5 py-4" style={{borderColor:"var(--line)"}}><h2 className="text-lg font-medium">通用设置</h2><p className="mt-1 text-sm" style={{color:"var(--ink-faint)"}}>当前仅保存基础期限；自动清理任务后续接入。</p></header><div className="grid md:grid-cols-2"><SettingGroup title="登录有效期" items={[["管理端登录有效天数","7"],["用户登录有效天数","30"]]}/><SettingGroup title="数据保存期" items={[["报告与调用链保存天数","180"],["PDF 保存天数","180"]]}/></div><footer className="flex justify-end border-t px-5 py-4" style={{borderColor:"var(--line-soft)"}}><button type="button" className="btn-gold flex items-center gap-2 px-5 py-2"><Save size={15}/>保存通用设置</button></footer></section>}

function SettingGroup({title,items}:{title:string;items:string[][]}){return <fieldset className="border-b p-5 md:border-b-0 md:odd:border-r" style={{borderColor:"var(--line-soft)"}}><legend className="font-medium">{title}</legend><div className="mt-4 space-y-4">{items.map(([label,value])=><label key={label} className="grid grid-cols-[1fr_7rem] items-center gap-4 text-sm"><span>{label}</span><input type="number" min="1" defaultValue={value} className="border px-3 py-2 text-right" style={{borderColor:"var(--line)",background:"var(--bg-card)"}}/></label>)}</div></fieldset>}
